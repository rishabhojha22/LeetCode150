public void kthLevel1(Node root,int level,int k){
    if(root==null)return ;
    if(level==k){
        System.out.print(root.data+" ");
        return;
    }
    kthLevel1(root.left,level+1,k);
    kthLevel1(root.right,level+1,k);
}

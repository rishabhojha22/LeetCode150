class Solution {
    public int singleNumber(int[] nums) {
        int ans=nums[0];
        int len=nums.length;

        for(int i=1;i<len;i++){
            ans^=nums[i];
        }

        return ans;
    }
}
